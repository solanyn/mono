name: Source release
on:
  push:
    branches: [main]

jobs:
  release:
    permissions:
      contents: write
      packages: write
    uses: ./.github/workflows/release.yml
    with:
      mode: source
    secrets: inherit
