name: Dependency release
on:
  schedule:
    - cron: "30 2 * * 0"
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
      packages: write
    uses: ./.github/workflows/release.yml
    with:
      mode: dependency
    secrets: inherit
