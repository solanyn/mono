---
title: Adding Cloud GPUs to My Home Kubernetes Cluster
description: How I added GPU burst computing to my home lab using Liqo and GKE spot instances
date: 2026-02-26
draft: true
tags: [kubernetes, gpu, liqo, gke, home-lab]
---

import Mermaid from "../../components/Mermaid.astro";

I run a Kubernetes cluster at home with three low-power Talos nodes, enough for most things, but when I want to train models or run GPU workloads, I don't have a good solution. I'm not interested in dropping money on a dedicated GPU for subpar performance.

What if we could just build something to burst to cloud GPUs when we need to?

## Sidequests

### Talos GPU Provider Operator

My first attempt was `tgp-operator`, what if we could just create a control plane for cloud providers? It went great! At first. Then we ran into problems. Provider API fragmentation meant lots of glue code and many of them didn't have API docs. I didn't want to front cash for capacity I might not use, some providers want you to load up a minimum number of credits before they'll let you even provision a GPU node. The biggest problem I had was, if my cluster is in a private network, how are we gonna make secure connections from the cloud to my cluster at home? I'm not putting my cluster endpoint publicly (neither would most on-prem cluster setups). Wireguard and Tailscale are awesome, but I really didn't want to build in networking fabric _just_ to use a cloud node. In the bin it goes.

### Modal

I've been keeping an eye on Modal since they launched, their infrastructure as SDK model is really cool. For my case, it's a bit too limiting, I can't schedule any pod I want and keeping everything in the Kubernetes ecosystem for monitoring reasons, turns out was pretty important to me. I did build (`modal-operator`)[!github.com/solanyn/modal-operator] with a CRD for a `ModalApp` that gets the Modal URL and slaps it in a Kubernetes service. But that only works for things like LLM endpoints and maybe other scale-to-zero suitable API services.

Kinda neat, but didn't entirely fit the bill.

## Liqo

I found Liqo when looking at CNCF projects. Maybe I needed cloud federation. Liqo peers clusters together to make them _relatively_ seamlessly. Kubernetes will happily scheudle onto Liqo nodes in a second cluster as "virtual nodes".

<Mermaid
  code={`graph LR
  subgraph Home["Home Cluster (Talos)"]
    API[API Server]
    N1[Node 1]
    N2[Node 2]
    N3[Node 3]
    VN[Virtual Node]
  end
  subgraph GKE["GKE (Remote)"]
    GPU1[T4 Spot Node]
    GPU2[T4 Spot Node]
  end
  API --> VN
  VN -.->|Liqo Tunnel| GPU1
  VN -.->|Liqo Tunnel| GPU2
`}
/>

Paired it with GKE using spot instances:

- Home cluster: always on, control plane, lightweight workloads
- GKE: small base node pool for Liqo pods, GPU node pool with spot T4s + general compute nodes, scales to zero when idle

### Offloading

When I submit a GPU workload, Liqo:

<Mermaid
  code={`sequenceDiagram
  participant User
  participant Home as Home API Server
  participant Liqo as Liqo Controller
  participant VN as Virtual Node
  participant GKE as GKE Cluster
  
  User->>Home: kubectl apply (GPU pod)
  Home->>Liqo: Pod needs nvidia.com/gpu
  Liqo->>VN: Schedule to virtual node
  VN->>GKE: Create pod via peering
  GKE->>GKE: Scale up T4 spot node
  GKE-->>VN: Pod running
  VN-->>Home: Status update
  Home-->>User: Pod ready
`}
/>

The pod runs on GKE hardware but appears in my home cluster and monitoring, ingress/egress work transparently.

### $$$

- Home cluster: I already dumped money here
- GKE control plane: 2x e2-small nodes, ~$70/mo, always on
- GPU nodes: spot T4 ~$0.35/hr, only when running
- General compute nodes: spot e2-medium ~X$/hr, only when running

## What's next

Now to pray cloud bills don't ruin my life. Maybe a Grafana dashboard to track how much money I pour into this in my [cluster](github.com/solanyn/home-ops). Thanks for reading!
